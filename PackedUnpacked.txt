DATA_SEG SEGMENT
    NUM DB 98H
DATA_SEG ENDS

CODE_SEG SEGMENT
    ASSUME CS: CODE_SEG, DS: DATA_SEG

START:MOV AX,DATA_SEG
      MOV DS,AX

      MOV AL,NUM
      AND AL,0FH
      MOV BL,AL
      MOV AL,NUM
      AND AL,0F0H
      MOV CL,04H
      ROR AL,CL
      MOV DL,AL

      MOV AH,4CH
      INT 21H

CODE_SEG ENDS
END START
