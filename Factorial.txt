DATA_SEG SEGMENT
   NUM DB 5
   FACT DB 0
DATA_SEG ENDS

CODE_SEG SEGMENT
   ASSUME CS: CODE_SEG, DS: DATA_SEG

START: MOV AX,DATA_SEG
       MOV DS,AX

       MOV AX,1
       MOV CX,NUM

BACK: MUL CX
      LOOP BACK
      MOV [FACT],AX

      MOV AH,4CH
      INT 21H

CODE_SEG ENDS
END START